# 1C Theme Engine — Список задач (на 2026-07-22)

## Статус
Тема **v5.4** (сборка 21:59, `ThemeHook3_v54.dll`) — актуальная рабочая версия. Живая проверка v5.0–v5.4 пройдена (`after26_v50.png` … `after30_v54.png`), каждая версия выгружалась чисто (`unloaded cleanly` со счётчиками в `themehook_v3_log.txt`).

Что нового в линейке v5:
- **v5.0** — EAT-патч провайдеров (cairo/gdi32/user32): поздно загружаемые модули получают хуки мгновенно, без ожидания RescanThread. Лог: все 19 хуков `EAT 1`. IAT-патч и re-scan оставлены страховкой.
- **v5.1** — pale-tint fix: цвета с min каналом > 0.55 — это фоны, а не акценты → жёлтая шапка F1 стала тёмной (гипотеза D подтверждена: дырка в карте, не кэш).
- **v5.2–5.3** — детект дублирующих пар через per-thread ring history (256 записей для cairo-глифов, 128 для GDI): emboss-тень и синтетический bold пропускаются (пары не смежны — нужна история).
- **v5.4** — ⚠️ ЖЁСТКИЙ УРОК: любая дорисовка/запись пикселей попадает в кэшированные surface и отравляет процесс навсегда (двоение переживало даже полную выгрузку; у v5.1 было 3477 deferred redraw). Режим **SKIP-ONLY**: deferred redraw и surf-remap удалены; незамапленный тёмный текст просто перекрашивается в #D0D0D0 (безопасно).

**Ждёт живой проверки пользователем v5.4:** читаемость текста справки и контекстного меню после ring-suppression — убедиться, что не «съедается» настоящий жирный текст (dup-skip ловит те же глифы+цвет со сдвигом 0.25–3px).

### Архив статуса v4.9 → v5.0
v5.0-EAT (сборка 20:05) активна в процессе 975792: выгружена v4.9, заинжекчена v5.0, скриншот `after26_v50.png` — весь UI тёмный, процесс стабилен. Гипотезы v5 (A–F) — в PROJECT_DOCS.md §8а.

## Баги
- [x] **F1-окно справки светлое / темнеет с задержкой** — исправлено в v5.0 (EAT-патч: поздние модули хукаются мгновенно, RescanThread больше не критичен).
- [x] **Жёлтая шапка F1-статей** — исправлено в v5.1 (pale-tint классификация: min канал > 0.55 → фон).
- [x] **Контекстное меню: двойной размазанный текст, белый бокс выделения** — GDI ghost-fix (FixupGdiTextColor) в v4.9 + GDI ring dup-suppression в v5.3; выделение idx13→#303062, idx14→#E8E8E8.
- [x] **Текст справки «двоится»** — emboss-пара (чёрный + белый проход со сдвигом 1px). Фикс: ghost-fix (#D0D0D0 для тёмного цвета глифов) + в v5.3–5.4 shadow-skip подтверждённой пары через ring history. Заголовки: синтетический bold WebKit — врождённый дефект, смягчён dup-skip.
- [x] **Ссылки справки нечитаемы** — `#536AC2`/`#0000EE`/`#0000CD` → `#729DBA`; visited `#551A8B → #B58756`.
- [x] Teal-акцент `#033041` → `#5D9DA5`.
- [x] **САМОБАГ: отравление кэш-поверхностей (v5.1)** — deferred redraw / surf-remap запекали двоение в кэш, переживавшее выгрузку DLL. Урок зафиксирован как инвариант SKIP-ONLY (v5.4): кэшированные surface не трогаем вообще.
- [ ] Проверить диалоги и формы (элемент справочника, палитра свойств, формы объектов) — собрать цвета, дополнить карту.
- [ ] Боксы вокруг ссылок в редакторе — если воспроизведутся после v5.1.

## Фичи
- [ ] **IFEO-автозагрузка** (гипотеза C, приоритет): Image File Execution Options + verifier DLL — ОС сама грузит тему при запуске 1cv8.exe. Без инжектора, без антивирусного риска CreateRemoteThread, хуки активны с нулевой секунды (кэши 1С создаются под маппингом).
- [ ] Гипотеза F: перерисовка без ресайза (InvalidateRect + UpdateWindow, либо WM_SETTINGCHANGE).
- [ ] JSON-профиль темы вместо вшитой карты (Dracula/One Dark/Monokai).
- [ ] GUI менеджера профилей (C# WPF) — фаза 2.

## Технические заметки
- 1cv8.exe — 32-битный; DLL x86 (build_v3.bat, MSVC 2022 Community).
- Рендер UI — Cairo (grphcs.dll, WebKit.dll): хуки cairo_set_source_rgb/rgba, cairo_show_glyphs/text; GDI: SetTextColor/SetBkColor/CreateSolidBrush/FillRect/DrawTextW/DrawTextExW; GetSysColor(Brush) — ролевой маппинг.
- EAT-патч: патчится AddressOfFunctions в таблице экспорта cairo.dll/gdi32.dll/user32.dll; откат через общий журнал патчей (IAT+EAT restore при DETACH).
- Ring history (v5.3+): cairo — 256 записей/поток (8 индексов глифов + x,y + цвет); GDI — 128 записей/поток (текст до 63 WCHAR + x,y + цвет). Сдвиг пары: 0.25–3px (cairo), 1–4px (GDI); same-position repaint (сдвиг 0) никогда не пропускается.
- Дерево vs редактор: cairo_clip_extents (клип < 700px, x1 < 500 → #333333).
- Выгрузка безопасна (IAT+EAT restore при DETACH, unload.py). Перерисовка — ресайзом (SetWindowPos).
- PaletteLog.dll тоже с IAT-restore — выгрузка не роняет процесс.

## Ключевые файлы
- `ThemeHook3.cpp` / `ThemeHook3.dll` — актуальная тема (**v5.4**)
- `ThemeHook3_v50…v54.dll`, `build_v50…v54.bat` — версионные сборки
- `inject.py`, `unload.py`, `screenshot.py`, `PaletteLog.dll`
- `palette_dump.txt` — собранные цвета 1С (включая лексемы кода и цвета справки)
- `themehook_v3_log.txt` — лог хуков (~8 МБ)
- `reference.png` — целевой референс (EDT Dark)
